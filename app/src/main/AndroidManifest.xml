<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wisnu.evoting">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Ui.Voting.AdapterCandidate"></activity>
        <activity android:name=".Ui.Login.LoginActivity"></activity>
        <activity android:name=".Ui.Dashboard.Dashboard"></activity>
        <activity android:name=".Ui.Hasil.HasilActivity"></activity>
        <activity android:name=".Ui.Profile.ProfileActivity"></activity>
        <activity android:name=".Ui.Voting.VotingActivity"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>